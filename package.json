{
  "scripts": {
    "start": "run-p watch serve",
    "tailwind": "tailwindcss -i src/css/tailwind.css -o src/css/style.css",
    "watch": "npm run tailwind -- --watch",
    "serve": "live-server ./src/ --port=8090"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.2.4"
  }
}
